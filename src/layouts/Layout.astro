---
interface Props {
	title?: string;
}

const { title } = Astro.props;

const titleConstructor = (title?: string) =>
	title ? `${title} • regularFleet` : `regularFleet`;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{titleConstructor(title)}</title>
	</head>
	<body class="min-h-dvh bg-slate-950 text-white">
		<header
			class="flex w-full py-4 sticky top-0 backdrop-blur-xl rounded-b-2xl border-b border-white/20"
		>
			<div class="flex items-center gap-4 px-4">
				<a href="/" class="uppercase text-xl font-bold">
					home
				</a>
				<div class="w-0.5 bg-white h-full"></div>
				<p>demo</p>
			</div>
			<div class="flex items-center gap-4 ml-auto px-4">
				<a
					href="/"
					class="opacity-30 hover:opacity-100"
				>
					vehicles
				</a>
				<a
					href="/supercharging"
					class="opacity-30 hover:opacity-100"
				>
					supercharging
				</a>
			</div>
		</header>
		<main class="my-4">
			<slot />
		</main>
	</body>
</html>
